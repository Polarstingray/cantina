[pytest]
# Pin the rootdir to backend/ and the suite to tests/. Besides being correct
# config, this stops pytest's config search from walking up past backend/ into
# parent directories (notably /home, which on some setups is a slow/stale
# automount that can hang a bare `stat`).
testpaths = tests
